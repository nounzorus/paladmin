{
  "name": "palworld-admin-panel",
  "version": "1.0.0",
  "description": "Interface web d'administration pour serveur dédié Palworld (API REST + RCON de secours)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "frontend:dev": "npm run dev --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "build": "npm run frontend:build"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "node-cron": "^4.6.0"
  }
}
